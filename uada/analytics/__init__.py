"""uada.analytics — P3 Advanced Analytics modules.

Each module is standalone and uses lazy imports for heavy dependencies
(scipy, scikit-learn, statsmodels) so the application starts without them.
"""
